name: build-and-push-image
on:
  push:
    branches:
      - "main"

permissions:
  packages: write
  contents: read

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to the Container registry
        uses: docker/login-action@v3
        with:
          registry: https://ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Meta
        id: meta
        run: |
          echo REPO_NAME=$(echo ${GITHUB_REPOSITORY} | awk -F"/" '{print $2}') >> $GITHUB_OUTPUT    
          echo REPO_VERSION=$(git describe --tags --always | sed 's/^v//') >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          target: runner
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ steps.meta.outputs.REPO_NAME }}:latest
            ghcr.io/${{ github.repository_owner }}/${{ steps.meta.outputs.REPO_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push migrate image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          target: migration
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ steps.meta.outputs.REPO_NAME }}:migrate-latest
            ghcr.io/${{ github.repository_owner }}/${{ steps.meta.outputs.REPO_NAME }}:${{ github.sha }}-migrate
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs:
      - build-and-push-image
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Get Meta
        id: meta
        run: |
          echo REPO_NAME=$(echo ${GITHUB_REPOSITORY} | awk -F"/" '{print $2}') >> $GITHUB_OUTPUT    
          echo REPO_VERSION=$(git describe --tags --always | sed 's/^v//') >> $GITHUB_OUTPUT

      - name: Login to the Container registry
        uses: docker/login-action@v3
        with:
          registry: https://ghcr.io
          username: ${{ secrets.DOCKER_PULL_USERNAME }}
          password: ${{ secrets.DOCKER_PULL_SECRET }}

      - name: SSH Key adden
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}

      - name: Deploy image
        env:
          DOCKER_HOST: "ssh://deploy@87.106.232.205"
          GIT_COMMIT_HASH: ${{ github.sha }}
          IMAGE_REPO: ghcr.io/${{ github.repository_owner }}/${{ steps.meta.outputs.REPO_NAME }}
          DOCKER_PULL_USERNAME: ${{ secrets.DOCKER_PULL_USERNAME }}
          DOCKER_PULL_SECRET: ${{ secrets.DOCKER_PULL_SECRET }}
        run: |
          ssh-keyscan -H "87.106.232.205" >> ~/.ssh/known_hosts
          echo "$DOCKER_PULL_SECRET" | docker login ghcr.io -u "$DOCKER_PULL_USERNAME" --password-stdin
          docker pull "${IMAGE_REPO}:${GIT_COMMIT_HASH}"
          docker pull "${IMAGE_REPO}:${GIT_COMMIT_HASH}-migrate"

          docker stack deploy -c stack.yaml --with-registry-auth minimal-payload-mui
          docker service update --detach=true --image "${IMAGE_REPO}:${GIT_COMMIT_HASH}-migrate" minimal-payload-mui_migrations
          docker service scale --detach=true minimal-payload-mui_migrations=1

          MIGRATION_OK=0
          for i in $(seq 1 60); do
            STATE=$(docker service ps minimal-payload-mui_migrations --no-trunc --format '{{.CurrentState}}|{{.Error}}' | head -n 1)
            echo "migrate-state: $STATE"

            if echo "$STATE" | grep -q '^Complete'; then
              MIGRATION_OK=1
              echo "Migration finished successfully."
              break
            fi

            if echo "$STATE" | grep -Eq '^(Failed|Rejected|Shutdown)'; then
              echo "Migration service failed."
              docker service logs minimal-payload-mui_migrations || true
              docker service scale --detach=true minimal-payload-mui_migrations=0 || true
              exit 1
            fi

            sleep 5
          done

          if [ "$MIGRATION_OK" -ne 1 ]; then
            echo "Migration did not finish in time."
            docker service logs minimal-payload-mui_migrations || true
            docker service scale --detach=true minimal-payload-mui_migrations=0 || true
            exit 1
          fi

          docker service scale --detach=true minimal-payload-mui_migrations=0
          docker service update --detach=true --image "${IMAGE_REPO}:${GIT_COMMIT_HASH}" minimal-payload-mui_app
